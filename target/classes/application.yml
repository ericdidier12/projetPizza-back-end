server:
  port: 8080
  servlet:
    context-path: /pizzeria

spring:
  datasource:
    username: postgres
    password: BuSI
    url: jdbc:postgresql://localhost:5432/pizzeriadb1
  jpa:
    database: POSTGRESQL
    generate-ddl: true
    hibernate.ddl-auto: update
    show-sql: true
security:
  basic:
    enabled: false
